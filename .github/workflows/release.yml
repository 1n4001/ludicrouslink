name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install Dependencies
      run: |
        cd frontend
        npm ci

    - name: Build Frontend
      run: |
        cd frontend
        npm run build

    - name: Upload Frontend Artifact
      uses: actions/upload-artifact@v4
      with:
        name: frontend-dist
        path: frontend/dist/

  build-backend:
    needs: build-frontend
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - os: linux
            arch: amd64
            output_name: ludicrouslink-linux-amd64
          - os: linux
            arch: arm64
            output_name: ludicrouslink-linux-arm64
          - os: linux
            arch: arm
            output_name: ludicrouslink-linux-arm
          - os: windows
            arch: amd64
            output_name: ludicrouslink-windows-amd64.exe
          - os: windows
            arch: arm64
            output_name: ludicrouslink-windows-arm64.exe
          - os: darwin
            arch: arm64
            output_name: ludicrouslink-darwin-arm64

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'
        cache-dependency-path: backend/go.sum

    - name: Download Frontend Artifact
      uses: actions/download-artifact@v4
      with:
        name: frontend-dist
        path: backend/public

    - name: Build Backend
      env:
        GOOS: ${{ matrix.os }}
        GOARCH: ${{ matrix.arch }}
        GOARM: 7
        CGO_ENABLED: 0
      run: |
        cd backend
        go mod tidy
        go build -ldflags="-s -w" -o ../${{ matrix.output_name }} .

    - name: Upload Backend Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.output_name }}
        path: ${{ matrix.output_name }}

  build-android:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Android App
      run: ./gradlew :ludicrouslinkAndroid:app:assembleDebug --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: ludicrouslink-debug.apk
        path: ludicrouslinkAndroid/app/build/outputs/apk/debug/app-debug.apk

  release:
    needs: [build-backend, build-android]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Download All Artifacts
      uses: actions/download-artifact@v4
      with:
        path: release-assets

    - name: Prepare Release Assets
      run: |
        mkdir -p release
        # Backend binaries
        cp release-assets/ludicrouslink-linux-amd64/ludicrouslink-linux-amd64 release/
        cp release-assets/ludicrouslink-linux-arm64/ludicrouslink-linux-arm64 release/
        cp release-assets/ludicrouslink-linux-arm/ludicrouslink-linux-arm release/
        cp release-assets/ludicrouslink-windows-amd64.exe/ludicrouslink-windows-amd64.exe release/
        cp release-assets/ludicrouslink-windows-arm64.exe/ludicrouslink-windows-arm64.exe release/
        cp release-assets/ludicrouslink-darwin-arm64/ludicrouslink-darwin-arm64 release/
        # Android APK
        cp release-assets/ludicrouslink-debug.apk/app-debug.apk release/ludicrouslink-debug.apk
        # Frontend dist as zip
        cd release-assets/frontend-dist && zip -r ../../release/frontend-dist.zip . && cd ../..
        ls -la release/

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        generate_release_notes: true
        files: |
          release/ludicrouslink-linux-amd64
          release/ludicrouslink-linux-arm64
          release/ludicrouslink-linux-arm
          release/ludicrouslink-windows-amd64.exe
          release/ludicrouslink-windows-arm64.exe
          release/ludicrouslink-darwin-arm64
          release/ludicrouslink-debug.apk
          release/frontend-dist.zip
